begin
  require 'jeweler'
  Jeweler::Tasks.new do |gemspec|
    gemspec.name = "delayed_job"
    gemspec.summary = "Database-backed asynchronous priority queue system -- Extracted from Shopify"
    gemspec.description = "Delated_job (or DJ) encapsulates the common pattern of asynchronously executing longer tasks in the background. It is a direct extraction from Shopify where the job table is responsible for a multitude of core tasks."
    gemspec.email = "tobi@leetsoft.com"
    gemspec.homepage = "http://github.com/tobi/delayed_job"
    gemspec.authors = ["Tobias Lütke"]
  end
rescue LoadError
  puts "Jeweler not available. Install it with: gem install jeweler"
end